server:
  port: 8002

spring:
  application:
    name: ai-review-service

# AI配置
#ai:
#  openai:
#    api-key: ${OPENAI_API_KEY:}
#    api-url: ${OPENAI_API_URL:https://apis.iflow.cn/v1/chat/completions}
#    model: Qwen3-Coder  # iFlow平台的代码专用模型，256k上下文

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
      service: ${spring.application.name}
    export:
      prometheus:
        enabled: true

